import {
    Example_DB
} from '../../../src/api/example/Example_DB';

describe('#math', () => {
    describe('Create', () => {
        it('', () => {
            new Example_DB().Create({});
        });
    });
});